"""Provider adapters. Each module implements documented provider HTTP APIs.

Live verification against real provider accounts is UNVERIFIED; behaviour is verified by
MockTransport contract tests (tests/unit/test_integrations_adapters.py).
"""
