"""PI controlled tools: the only path from PI agents to business data.

Import from the submodules (``catalog``, ``registry``) directly; this package module stays
empty so configuration code can read the catalog without import cycles.
"""
